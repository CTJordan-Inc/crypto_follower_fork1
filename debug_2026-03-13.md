# Debug 紀錄

## 日期
- 2026-03-13

## 問題
- 隨機找 50 地址時失敗：
  - `network query failed: etherscan block lookup failed: NOTOK Error! Block timestamp too far in the future`

## Root Cause
- `_resolve_block_range()` 原本會把 `end_date` 直接組成 `23:59:59 UTC`
- 若使用者選的是「今天」但當下 UTC 尚未到當天結尾，或直接選到未來日期
- 就會把一個未來 timestamp 丟給 Etherscan `getblocknobytime`
- Etherscan 會回：
  - `Block timestamp too far in the future`

## 修正
- `app/services/network_sync.py`
  - 新增 `_resolve_effective_end_date()`
  - `end_date` 先 clamp 到目前 UTC 時間，再做 block lookup
  - `sync_address_from_network_and_recompute()` 改用 `effective_end_date`
  - 若 `start_date` 本身已在未來，明確回錯 `start_date cannot be in the future`

## 驗證
- 新增測試：
  - `tests/test_network_sync.py`
  - `test_resolve_effective_end_date_clamps_future_timestamp`
- 全量測試通過：
  - `.venv/bin/pytest -q`
  - `20 passed`

## 問題
- 點擊「查看詳情」時失敗：
  - `GET /api/v1/performance/saved/{id}`
  - `500 Internal Server Error`
  - `fastapi.exceptions.ResponseValidationError`

## Root Cause
- `SavedAnalysisSnapshot.payload` 內有一批舊快照是在新增地址市值欄位之前保存的
- 舊 payload 的 `meta` 缺少以下必填欄位：
  - `address_market_cap_usd`
  - `address_market_cap_basis`
  - `address_peak_nav_usd`
  - `address_average_nav_usd`
- `/api/v1/performance/saved/{id}` 直接回傳原始 payload
- `PerformanceResponse` 目前把上述欄位設為必填，因此 FastAPI 在 response validation 階段直接拋 500

## 修正
- `app/services/analysis_store.py`
  - 新增 `hydrate_saved_performance_payload()`
  - 對舊快照回傳前做向下相容補值
  - 補值來源使用 `nav_curve` 反推：
    - `address_peak_nav_usd` = 區間內最大 NAV
    - `address_average_nav_usd` = 區間內平均 NAV
    - `address_market_cap_basis` 預設為 `max_nav`
    - `address_market_cap_usd` 依 basis 對應回填
  - `save_analysis_snapshot()` 也改成先做 hydration，避免新舊 payload 口徑再分叉
- `app/api/routes/performance.py`
  - `get_saved_analysis()` 改成回傳 hydrated payload，不再直接回原始 JSON

## 驗證
- 新增測試：
  - `tests/test_analysis_store.py`
  - `test_hydrate_saved_performance_payload_backfills_legacy_market_cap_meta`
- 全量測試通過：
  - `.venv/bin/pytest -q`

## 問題
- 點擊「隨機產生 50 地址」時，前端仍會顯示：
  - `失敗：請求逾時，請稍後再試`

## Root Cause
- 這不是單純 API key 壞掉
- 目前按鈕預設帶 `最低地址市值 = 10000 USD`
- `generate_random_recent_addresses()` 會對沒有快取的候選地址逐一做 `_estimate_address_market_cap_from_network()`
- 舊實作對每個陌生地址都可能觸發：
  - `getblocknobytime`
  - `txlist`
  - `tokentx`
  - CoinGecko 現貨價查詢
- 而且 `_load_network_snapshots()` 在沒有 base snapshot 時會走完整持倉重建路徑
- 在免費額度與限流下，這種「隨機取樣前先做多地址預分析」本質上很容易超過前端 `180s` timeout

## 修正
- `app/services/network_sync.py`
  - `generate_random_recent_addresses()` 對陌生地址的 network screening 增加硬上限
  - 新增 `random_address_network_screen_limit`，預設只對少量候選地址做鏈上快速估值
  - 新增 `random_address_screen_max_pages`，預篩只抓區間內有限頁數事件，不再走高成本完整重放
  - `_fetch_etherscan_events()` 新增 `max_pages` 參數供快速預篩使用
- `app/core/config.py` / `.env.example`
  - 補上對應設定
- `app/templates/index.html` / `app/static/dashboard.js`
  - UI 文案改成「快速預篩」，避免使用者誤以為這是精確完整分析

## 驗證
- 新增測試：
  - `tests/test_network_sync.py`
  - `test_generate_random_recent_addresses_limits_unsaved_network_screening`
- 全量測試通過：
  - `.venv/bin/pytest -q`

## 實測補充
- 以 live network 直接呼叫 `generate_random_recent_addresses()` 重現後，確認 timeout 不是前端誤判
- 真正卡點在：
  - `_estimate_address_market_cap_from_network()`
  - `_fetch_spot_prices()`
  - `_fetch_spot_prices_chunk()`
- 實際 trace 顯示：
  - Etherscan block range / `txlist` / `tokentx` 通常都在幾秒內完成
  - 慢點發生在 CoinGecko `simple/token_price`
  - random screening 模式下，單一地址仍可能因為：
    - 壞 contract 導致 400 拆批遞迴
    - 429 rate limit 觸發 backoff
    - 兩者疊加後把整個請求拖到前端 timeout

## 第二次修正
- `app/services/network_sync.py`
  - random screening 的 spot price 查詢新增 `contract_limit`
  - random screening 模式下只抓少量 spot token（`random_address_screen_spot_token_limit`）
  - random screening 模式下，CoinGecko `400` 不再遞迴拆批
  - random screening 模式下，CoinGecko `429/5xx` 不再長時間重試，直接 fast-fail 回空價格
- `app/core/config.py` / `.env.example`
  - 新增 `random_address_screen_spot_token_limit`

## Live 驗證結果
- 修正後以 live network 實測：
  - `count=50`
  - `min_market_cap_usd=10000`
  - `exclude_saved=true`
  - 約 `29.06s` 返回
  - 成功返回 `1` 個地址
- 結論：
  - timeout 已解掉
  - 但在目前「快速預篩 + 嚴格過濾」條件下，回傳數量可能明顯少於 50，這是速度與嚴格度的 tradeoff

# Debug Log - 2026-03-06

## 問題描述
- 使用者在 `POST /api/v1/performance/{address}/network/recompute` 收到 `400 Bad Request`。
- 回傳錯誤訊息：
  - `etherscan request failed: Result window is too large, PageNo x Offset size must be less than or equal to 10000`

## 重現線索
- 服務日誌顯示：
  - `POST /api/v1/performance/<address>/network/recompute HTTP/1.1" 400 Bad Request`
- 當前實作在 Etherscan 查詢使用：
  - `offset = 10000`
  - `page` 從 `1` 逐頁遞增

## 根因分析
- Etherscan API 限制 `page * offset <= 10000`。
- 既有實作在第 2 頁時變成 `2 * 10000 = 20000`，直接違反限制，故 API 回錯。

## 修復內容
1. 新增安全分頁計算 `_resolve_etherscan_pagination()`：
   - 依 Etherscan window 限制自動計算安全的 `offset/pages`。
   - 強制滿足 `page * offset <= 10000`。
2. 新增設定 `etherscan_page_offset`（預設 `1000`），避免高 offset 觸發錯誤。
3. `_fetch_etherscan_events()` 改用安全分頁參數。
4. 若仍遇到 window error，加入降級重試機制（縮小 offset）。

## 變更檔案
- `app/services/network_sync.py`
- `app/core/config.py`
- `.env.example`
- `README.md`
- `tests/test_network_sync.py`

## 驗證
- 進行語法檢查：
  - `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile $(find app tests -name '*.py' -type f)`
  - 結果：通過

## 備註
- `.env.example` 中不應放真實 API key，已改回空值。
- 這次 debug 紀錄依要求獨立放在 `debug_2026-03-06.md`，未寫入 `progress.md`。

---

## 追加問題（同日）
- 使用者回報仍失敗，錯誤為 CoinGecko `429 Too Many Requests`。

### 錯誤訊息
- `network query failed: Client error '429 Too Many Requests' ... /coins/ethereum/contract/.../market_chart/range`

### 根因分析
1. 原流程對每個持倉 token 都打一次歷史價格 API。
2. 某些地址（例如熱門地址）token 非常多，短時間大量請求導致 CoinGecko 節流。

### 第二輪修復內容
1. 新增 CoinGecko 節流保護
   - `429/5xx` 指數退避重試（含 `Retry-After` 支援）。
   - 請求間隔控制（避免突發流量）。
2. 新增 spot price 批次查詢
   - 先用 `/simple/token_price` 批次抓現價估值，降低歷史查價 token 數量。
3. 改為只抓「穩定幣 + 原生幣 + Top N 估值代幣」的歷史價格
   - 避免對大量 spam/dust token 發送歷史查價請求。
4. 穩定幣價格 fallback
   - 對 `USDT/USDC/DAI/FDUSD` 直接填 `1.0` 日價格序列。
5. 擴充設定
   - `COINGECKO_API_KEY`
   - `COINGECKO_MAX_RETRIES`
   - `COINGECKO_REQUEST_INTERVAL_SECONDS`
   - `COINGECKO_CONTRACT_CHUNK_SIZE`
   - `COINGECKO_SPOT_CONTRACT_LIMIT`
6. 429 錯誤改為可行動訊息
   - 回覆 `coingecko rate limit reached (HTTP 429); please wait and retry, or set COINGECKO_API_KEY`

### 第二輪變更檔案
- `app/services/network_sync.py`
- `app/core/config.py`
- `.env.example`
- `README.md`
- `tests/test_network_sync.py`

### 第二輪驗證
- 語法檢查：
  - `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile $(find app tests -name '*.py' -type f)`
  - 結果：通過

---

## 追加問題（同日第三輪）
- 使用者回報 Etherscan 速率限制：
  - `NOTOK Max calls per sec rate limit reached (3/sec)`

### 第三輪根因
1. Etherscan 交易頁面抓取仍可能在短時間連續請求。
2. 原本只處理分頁視窗限制，尚未完整處理 Etherscan 速率限制節流與重試。

### 第三輪修復內容
1. Etherscan 請求節流
   - 每次 Etherscan request 前加入可配置間隔（預設 `0.35s`）。
2. Etherscan rate-limit 重試
   - 對 `HTTP 429`、`5xx`、以及 payload 內的 `Max calls per sec rate limit reached` 訊息，採指數退避重試。
3. 可行動錯誤訊息
   - 重試耗盡時回傳：
     - `etherscan rate limit reached (3/sec); please retry, or lower ETHERSCAN_MAX_PAGES / increase ETHERSCAN_REQUEST_INTERVAL_SECONDS`
4. 擴充設定
   - `ETHERSCAN_MAX_RETRIES`
   - `ETHERSCAN_BACKOFF_SECONDS`
   - `ETHERSCAN_REQUEST_INTERVAL_SECONDS`

### 第三輪變更檔案
- `app/services/network_sync.py`
- `app/core/config.py`
- `.env.example`
- `README.md`
- `tests/test_network_sync.py`

### 第三輪驗證
- 語法檢查：
  - `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile $(find app tests -name '*.py' -type f)`
  - 結果：通過

---

## 追加問題（同日第四輪）
- 使用者回報 CoinGecko `simple/token_price` 回傳 `400 Bad Request`。
- 失敗 URL 顯示大量 `contract_addresses` 一次打包查詢。

### 第四輪根因
1. 批次 spot 查詢的 `contract_addresses` 可能包含無效或不被 API 接受的地址，造成整批 400。
2. 單批過大時，也可能踩到上游服務對 URL/參數的限制。

### 第四輪修復內容
1. 新增 `simple/token_price` 自動拆批遞迴
   - 遇到 `400` 時，將該批次二分再試。
   - 若拆到單一合約仍 `400`，直接略過該合約，不阻斷整體查詢。
2. 降低預設批次規模
   - `COINGECKO_CONTRACT_CHUNK_SIZE` 由 `80` 下調到 `40`。
   - `COINGECKO_SPOT_CONTRACT_LIMIT` 由 `600` 調整為 `300`。
3. 文件補充
   - README 補上 `400` 情境與自動拆批說明。

### 第四輪變更檔案
- `app/services/network_sync.py`
- `app/core/config.py`
- `.env.example`
- `README.md`
- `tests/test_network_sync.py`

### 第四輪驗證
- 語法檢查：
  - `PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile $(find app tests -name '*.py' -type f)`
  - 結果：通過

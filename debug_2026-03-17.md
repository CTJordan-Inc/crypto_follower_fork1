# Debug 紀錄

## 日期
- 2026-03-17

## 問題
- 使用者回報：
  - 先產出 50 個地址
  - 再按下批量分析
  - 8 小時後前端仍無任何結果

## Root Cause
- Dashboard 的批量分析仍然直接呼叫：
  - `POST /api/v1/performance/batch/network/recompute`
- 後端 `batch_load_or_sync_address_performance()` 會同步串行處理整批地址
- 只有全部地址都完成後，HTTP response 才會回到前端
- 對 50 個隨機且大多數未快取的地址來說，這條路徑會觸發大量：
  - Etherscan 歷史事件同步
  - CoinGecko 日價格查詢
  - NAV 重算與保存
- 結果是：
  - 前端長時間看不到任何中間結果
  - 單一慢地址就足以拖住整批 response
  - UX 上等同「卡死」

## 修正
- 不再讓 Dashboard 一次等待整批 batch API
- 改成前端逐地址呼叫：
  - `POST /api/v1/performance/{address}/network/recompute`
- 每完成一個地址，就立即：
  - 更新表格
  - 更新進度文字
  - 累積成功 / 失敗數
- 另外加上單地址 batch 模式 timeout：
  - 避免單一地址把整批流程無限拖住

## 影響
- 總 wall-clock time 不一定大幅縮短
- 但使用者不再遇到「8 小時沒有任何結果」的黑盒狀態
- 會改成可觀察的進度式 UX：
  - 已處理 N / 50
  - 已成功 X
  - 已失敗 Y

## 相關檔案
- `app/static/dashboard.js`
- `app/templates/index.html`
- `README.md`
